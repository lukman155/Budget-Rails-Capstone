<div class="card my-4">
  <div class="card-body">
    <%= render @bill %>
  </div>
</div>

<div class="d-flex justify-content-end">
  <%= link_to "Back to bills", category_bills_path(@category), class: "btn btn-secondary" %>
</div>
